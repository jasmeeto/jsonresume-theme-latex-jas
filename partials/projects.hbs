{{#if resume.projects.length}}
\subsectioncustom{Personal Projects}
\vspace{-0.5em}
\begin{flushleft}
    {
        {{#if resume.basics.projectUrl}}
        \textcolor{greyish}{ \href{ {{resume.basics.projectUrl}} }{ {{resume.basics.projectUrl }} }}
        {{/if}}
    }
    {{#each resume.projects}}
        \\
        {
        \href{ {{url}} }{\textbf{ {{name}} }}: {{description}}
        }
    {{/each}}
\end{flushleft}

{{/if}}


