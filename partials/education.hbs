{{#if resume.education.length}}

\subsectioncustom{Education}
\begin{flushleft}
	{{#each resume.education}}
    \schoolsectionend
    {
      {{institution}}
    }
    {
    {{city}}
    }
    {
    {{studyType}}
    }
    {
    {{#if startDate}}
        {{dateFormat startDate format="MMM YYYY"}}
    {{/if}}
    {{#if endDate}}
        - {{dateFormat endDate format="MMM YYYY"}}
    {{else}}
        - Present
    {{/if}}
    }
    {
        {{#if courses.length}}
        \textbf{Relevant Courses:}
        {{#each courses}}
            {{#if @index}}, {{/if}} {{.}}
        {{/each}}
        {{/if}}
    }
    {{/each}}
\end{flushleft}
{{/if}}
